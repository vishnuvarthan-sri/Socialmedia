<!DOCTYPE html>
<html lang="en">

<head>
   
    <style>
        form {
            border: 3px solid #4fded7;
        }
        body {
            font-family: Arial;
            padding: 20px;
            background: #f1f1f1;
        }
       input[type=submit] {
            background-color: dimgrey;
            color: white;
            padding: 12px 10px;
            margin: 8px 0;
            border: none;
            cursor: pointer;
            width: 20%;
            opacity: 0.9;
        }
        .header {
            padding: 10px;
            font-size: 10px;
            text-align: left;
            background-color:dimgrey;
        }
        .card {
            background-color: white;
            padding: 20px;
            margin-top: 20px;
        }
      
        label {
            padding: 12px 12px 12px 0;
            display: inline-block;
        }
        input[type=text], select, textarea {
            width: 50%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 4px;
            resize: vertical;
        }
        .col-25 {
            float: left;
            width: 75%;
            margin-top: 6px;
        }

       
        textarea {
            width: 100%;
            height: 150px;
            padding: 12px 20px;
            box-sizing: border-box;
            border: 2px solid #ccc;
            border-radius: 4px;
            background-color: #f8f8f8;
            resize: none;
        }
        .imgcontainer {
            float: right;
            text-align: center;
            border-radius: 50%;
            margin-top: 6px;
        }
        .topright {
            position: absolute;
            top: 8px;
            right: 16px;
            font-size: 18px;
        }

        img.avatar {
            width: 30%;
            border-radius: 30%;
        }
        @media screen and (max-width: 800px) {
            .col-25,  input[type=submit] {
                width: 100%;
                margin-top: 0;
                padding: 0;
            }
        }
        .row:after {
            content: "";
            display: table;
            clear: both;
        }
        
    </style>
</head>

<body>
    <div class="header">
        <h1>POST</h1>
    </div>
    <form action="/posts/detail/:id" method="post">
        <div class="card">
            <div class="row">

                <div class="col-25">
                    <label for="description"> POST DESCRIPTION:</label>
                    <input type="text" id="description" placeholder="descripion of post" />
                </div>
                <div  class="imgcontainer">
                    <input type="file" accept="image/*" name="image" id="file" onchange="loadFile(event)" style="display: none;">
                    <label for="file" style="cursor: pointer;">Image</label>
                    <img id="output" class="avatar" />

                    <script>
                        var loadFile = function (event) {
                            var image = document.getElementById('output');
                            image.src = URL.createObjectURL(event.target.files[0]);
                        };
                    </script>
                </div>

                <div class="col-25">
                    <label for="post">Post: </label>
                    <textarea id="post" name="post" placeholder="post here " rows="20" cols="40"></textarea><br />
                </div>
            </div>
        </div>
        <div class="col-25">
            <input type="submit" value="Post" onClick="window.location.href='/views/page.ejs'" />
        </div>
        <div class="topright">
         <input type="submit" formaction="/logout"   value="logout" />
        </div>
    </form>
    

</body>
</html>